import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.repartidor.app',
  appName: 'Repartidor',
  webDir: 'dist/browser',
  bundledWebRuntime: false
};

export default config;
