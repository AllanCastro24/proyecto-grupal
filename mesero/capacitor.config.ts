import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mesero.app',
  appName: 'Mesero',
  webDir: 'dist/browser',
  bundledWebRuntime: false
};

export default config;
